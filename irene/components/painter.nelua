require 'nene.colors'
require 'irene.makers'

local Painter = @record{
  color: Color,
  active: boolean,
  use_lines: boolean,
}
## Irene.impl_is_component(Painter)

global Irene.Comps.Painter = @Painter
