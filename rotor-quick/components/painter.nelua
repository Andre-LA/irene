require 'rotor-quick.makers'

global RotorQuick.Components.Painter = @record{
  color: Color,
  active: boolean,
}

local Painter = RotorQuick.Components.Painter
## is_component(Painter)

## RQ_AddMaskAndStorage('painter', Painter)
global Painter.mask = RotorQuick.masks.painter
global Painter.storage = &RotorQuick.storages.painter
