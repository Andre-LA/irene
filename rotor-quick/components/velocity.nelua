require 'rotor-quick.makers'

global RotorQuick.Components.Velocity = @record{
  direction: Vector2,
  speed: number,
  active: boolean,
}

local Velocity = RotorQuick.Components.Velocity
## is_component(Velocity)

## RQ_AddMaskAndStorage('velocity', Velocity)
global Velocity.mask = RotorQuick.mask_map.velocity
global Velocity.storage = &RotorQuick.storage_map.velocity
