require 'rotor-quick.makers'

global RotorQuick.Components.IsStatic = @record{
  placeholder: cchar,
}

local IsStatic = RotorQuick.Components.IsStatic
## is_component(IsStatic)

## RQ_AddMaskAndStorage('is_static', IsStatic)
global IsStatic.mask = RotorQuick.masks.is_static
global IsStatic.storage = &RotorQuick.storages.is_static
